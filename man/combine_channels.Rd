% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine.R
\name{combine_channels}
\alias{combine_channels}
\title{Combine channel pulls into a unified weekly dataset}
\usage{
combine_channels(..., ga4 = NULL, config = NULL)
}
\arguments{
\item{...}{One or more per-channel tibbles (output of \code{pull_meta()},
\code{pull_google_ads()}, \code{pull_linkedin()}, or any other channel that
conforms to \code{channel_weekly_schema}).}

\item{ga4}{Optional weekly GA4 tibble from \code{pull_ga4_sessions()} or
\code{pull_ga4_pagepath()}.}

\item{config}{A \code{campaign_config}. If supplied, the returned object gets
its campaign name attached for downstream reporting.}
}
\value{
A list with three named tibbles:
\describe{
\item{spend_long}{One row per (week, channel). Includes
\code{platform_conversions} which should be used only within-channel.}
\item{weekly_total}{One row per week with cross-channel totals for
spend, impressions, and clicks only (not conversions \if{html}{\out{<e2>}}<80><94> see note
above).}
\item{weekly_combined}{\code{weekly_total} left-joined to \code{ga4} and
enriched with \code{cost_per_session} and \code{cost_per_ga4_conversion}.}
}
}
\description{
Combine channel pulls into a unified weekly dataset
}
