% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_meta_insights.R
\name{fetch_meta_insights}
\alias{fetch_meta_insights}
\title{Fetch Meta (Facebook) ad insights from the Marketing API}
\usage{
fetch_meta_insights(
  date_from,
  date_to,
  time_increment = "7",
  report_level = "campaign",
  fb_account,
  fb_access_token,
  api_version = "v20.0"
)
}
\arguments{
\item{date_from, date_to}{Date strings (\code{YYYY-MM-DD}) bounding the report.}

\item{time_increment}{Either \code{"all_days"} (default) or an integer-string
like \code{"7"} for weekly buckets.}

\item{report_level}{One of \code{"account"}, \code{"campaign"}, \code{"adset"}, \code{"ad"}.
Defaults to \code{"campaign"}.}

\item{fb_account}{The ad account ID (with the \code{act_} prefix).}

\item{fb_access_token}{A Facebook Graph API access token with
\code{ads_read} scope on the account.}

\item{api_version}{Marketing API version, e.g. \code{"v20.0"}.}
}
\value{
A tibble with character columns for the IDs/names and numeric
columns for everything else, plus parsed \code{date_start} / \code{date_stop}.
Empty tibble if Meta returns no rows.
}
\description{
Calls the Marketing API \verb{/insights} endpoint for an ad account, paginating
through the result set, and returns a wide tibble with one row per
(campaign or ad, time bucket).
}
\details{
Conversion data is reported under Meta's \code{actions} and \code{action_values}
arrays. This function pivots only the \code{purchase} action type into wide
columns: \code{count.purchase}, \code{value.purchase}, \verb{1d_view.purchase},
\verb{7d_click.purchase}, etc. If you need other action types or windows,
modify the \code{purchase} filters below or add post-processing.
}
\keyword{internal}
