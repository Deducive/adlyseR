% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pull_transactions.R
\name{join_spend_and_revenue}
\alias{join_spend_and_revenue}
\title{Join paid-channel spend against attributed transaction revenue}
\usage{
join_spend_and_revenue(spend_long, transactions, which = c("fc", "lc"))
}
\arguments{
\item{spend_long}{Output of \code{combine_channels()$spend_long}.}

\item{transactions}{Output of \code{pull_transactions()}.}

\item{which}{\code{"fc"} (default) or \code{"lc"} attribution.}
}
\value{
A tibble per (week, channel) with \code{spend}, \code{impressions}, \code{clicks},
\code{transactions}, \code{units}, \code{revenue}, and \code{roas}. Non-paid channels
(organic / direct / email / referral / unattributed / other) appear
with spend = 0; paid channels with no attributed revenue get revenue = 0.
}
\description{
Produces a channel-level view combining adlyseR's \code{spend_long} with the
weekly attributed-revenue tibble. Useful for ROAS and assist/close tables.
}
