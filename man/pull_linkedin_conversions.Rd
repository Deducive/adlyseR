% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pull_linkedin.R
\name{pull_linkedin_conversions}
\alias{pull_linkedin_conversions}
\title{Pull LinkedIn conversions by conversion rule}
\usage{
pull_linkedin_conversions(
  config,
  refresh = FALSE,
  debug = FALSE,
  date_from = config$start_date,
  date_to = config$end_date,
  campaign_name_groups = NULL
)
}
\arguments{
\item{config}{A \code{campaign_config} object. Required: \code{linkedin.account_id}.
Scope: at least one of \code{linkedin.campaign_groups} (vector of group IDs)
or \code{linkedin.campaigns} (vector of campaign IDs).
Optional: \code{linkedin.api_version} (default \code{"202509"}),
\code{linkedin.token_file}, \code{linkedin.token_refresh_script}.}

\item{refresh}{Force re-fetch instead of loading from cache.}

\item{debug}{If \code{TRUE}, prints the full request URL and the first 500
chars of the response body for every scope. Useful for diagnosing
empty-result cases.}

\item{date_from, date_to}{Date range to pull. Defaults to the campaign
config's start and end dates.}

\item{campaign_name_groups}{Optional token groups passed to
\code{\link[=match_campaign_name]{match_campaign_name()}}. When supplied, LinkedIn campaigns are discovered
by name from the ad account instead of relying on configured campaign
groups.}
}
\value{
A weekly tibble with campaign and conversion-rule identifiers plus
\code{externalWebsiteConversions}, \code{externalWebsitePostClickConversions}, and
\code{externalWebsitePostViewConversions}.
}
\description{
Queries LinkedIn's Marketing API at campaign/conversion grain, aggregating
daily conversion-rule metrics into campaign weeks. This is a companion to
\code{\link[=pull_linkedin]{pull_linkedin()}}: it does not include spend, impressions, or clicks because
adding \code{CONVERSION} as a pivot changes the row grain.
}
